What changed: Three novel techniques combined. (1) CONTRASTIVE SELF-CHECK: 'Before calling any tool: is this read-only? If yes, stop — act directly instead.' This is a metacognitive prompt that forces the model to audit its own tool choice, targeting TC-3/TC-10 which call lookup/eligibility. (2) ACT IMMEDIATELY from exp 010 combined with COMPUTE REFUNDS from exp 011: 'Compute refunds and act immediately' — targeting TC-5 which computes correctly in text but doesn't call the tool. (3) MULTI-TOPIC FOCUS: 'For multi-topic messages, resolve the top financial action only' — from exp 013, targeting TC-3/TC-10 which have 3-4 sub-requests causing attention dilution. (4) Also replaced explicit tool names (check_eligibility, lookup_order) with concept 'read-only' to stay within prompt constraints.

Why: Best (exp 011, 0.55) fails on TC-3,4,5,10. TC-3 and TC-10 call lookup/eligibility despite 'never call' instruction. TC-4 asks for verification in text. TC-5 computes in text but doesn't act. Novel insight: the model doesn't disobey the instruction — it doesn't realize that check_eligibility IS a lookup tool. The self-check forces it to classify the tool type before calling it.

Thesis: Self-check should fix TC-3,10 by making model realize eligibility/lookup are read-only. Act-immediately + tool-only should fix TC-5. Multi-topic focus should reduce attention dilution. TC-4 still risky — 'unverified' step may not fire. Expecting 0.65+.

Learnings: Exp 011 showed 'never call lookup tools' works for most cases but fails when attention is diluted. Exp 013 (router identity) over-constrained and broke TC-1,9. The self-check is a lighter intervention — it doesn't change the agent's identity but adds a metacognitive step. Key insight from cognitive science: telling someone 'don't do X' is weaker than 'before doing anything, check if it's X.'

If fails: Try making the self-check even more explicit with an if-then pattern: 'IF the tool you're about to call doesn't change the account, THEN you're making a mistake.' Or try chain-of-thought: 'Think: what rule applies? Then call that tool.'